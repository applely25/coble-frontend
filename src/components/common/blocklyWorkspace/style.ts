import { BlocklyWorkspace } from 'react-blockly';
import styled from 'styled-components';

export const BlocklyContainer = styled.div`
  height: 90vh;
  width: 100%;
`;
export const BlocklyWorkspaceContainer = styled(BlocklyWorkspace)`
  height: 100%;
  width: 100%;
`;
